name: Manual trigger, no inputs, not tags

on:
  workflow_dispatch:
    description: 'sample'

jobs:
  maual-trigger-sample:
    runs-on: ubuntu-latest

    steps:
    - run: echo "Repo name is ${{ github.event.repository.name }}"

