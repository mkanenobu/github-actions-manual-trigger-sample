name: Manual trigger sample

on:
  workflow_dispatch:
    inputs:
      exit_code:
        description: 'exit code'
        required: true
        default: '0'
    description: 'sample'

jobs:
  maual-trigger-sample:
    runs-on: ubuntu-latest

    steps:
    - run: echo "Repo name is ${{ github.event.repository.name }}"
    - run: bash exit_with_args.bash "${{ github.event.inputs.exit_code }}"

